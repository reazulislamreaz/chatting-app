{
  "name": "chatting-app",
  "private": true,
  "version": "1.0.0",
  "description": "Real-time chat application monorepo",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix chatting-app-backend\" \"npm run dev --prefix chatting-app-frontend\"",
    "dev:backend": "npm run dev --prefix chatting-app-backend",
    "dev:frontend": "npm run dev --prefix chatting-app-frontend",
    "build": "npm run build --prefix chatting-app-backend && npm run build --prefix chatting-app-frontend",
    "install:all": "npm install && npm install --prefix chatting-app-backend && npm install --prefix chatting-app-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
